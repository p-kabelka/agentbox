# Agent preset — copied to .sandbox/sessions/<name>/config/agent.yaml on 'sandbox run --name <name>'.

# Dotfiles placed in the agent's $HOME at session start.
# Keys are paths relative to home (e.g. .bashrc, .gitconfig, .config/git/config).
dotfiles: {}
  # .bashrc: |
  #   export PS1="\u@sandbox:\w\$ "
  #   alias ll='ls -la'
  # .gitconfig: |
  #   [user]
  #       name = Agent
  #       email = agent@sandbox
